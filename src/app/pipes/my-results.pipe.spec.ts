import { MyResultsPipe } from './my-results.pipe';

describe('MyResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new MyResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
